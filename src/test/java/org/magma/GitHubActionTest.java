package org.magma;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * This class only exists to check that the GitHub Actions
 * pipeline has been set up correctly.
 */
public class GitHubActionTest {
	/**
	 * Simplest test possible that verifies that tests pass.
	 */
	@Test
	void test() {
		assertTrue(true);
	}
}
